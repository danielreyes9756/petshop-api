<p>
  <strong>Title:</strong>
  <%= @size.title %>
</p>

<p>
  <strong>Color:</strong>
  <%= @size.color %>
</p>

<%= link_to 'Edit', edit_size_path(@size) %> |
<%= link_to 'Back', sizes_path %>
